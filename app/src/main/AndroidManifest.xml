<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trazins.trazinsdroidpre">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_trazins_white_nbg"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_trazins_white_nbg"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".PrinterSettingsActivity" />
        <activity android:name=".settingsactivities.PrinterSettingsActivity" />
        <activity android:name=".MaterialPostCounterActivity" />
        <activity android:name=".SurgicalProcessPreviousDataActivity" />
        <activity android:name=".SurgicalProcessActivity" />
        <activity android:name=".ShipmentActivity" />
        <activity android:name=".SelectionActivity" />
        <activity android:name=".LocateActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>